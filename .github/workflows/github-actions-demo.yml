name: C/C++ CI

on:
  push:
    branches: '**'
  pull_request:
    branches: '**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: dependencies
      run: sudo apt-get update && sudo apt-get install cmake -y
    - name: recipe
      run: ls && cmake -B hw-01/build/ -S hw-01/ -DPATCH_VERSION=${{ github.run_number }}
    - name: build
      run: ls && cmake --build hw-01/build
    - name: run
      run: ls && ./hw-01/build/helloworld_cli ${{github.run_number}}
    - name: release
      uses: actions/create-release@v1
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
